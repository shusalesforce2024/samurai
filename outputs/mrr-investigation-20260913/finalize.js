const fs=require('fs');const p='outputs/mrr-investigation-20260913/';let a=fs.readFileSync(p+'P0候補26件_参照ID紐付け.csv','utf8').trim().split(/\r?\n/);let out=[a[0]+',確定可能性,追加取得理由'];for(let i=1;i<a.length;i++){let r=a[i].split(',');let monthly=r[3]||'',prod=r[4]||'',inv=r[5]||'',cl=r[8]||'';let can=monthly&&prod&&inv;let reason=can?'':'見積明細IDまたはFreee移行ワークID、または参照欠落の業務確認が必要';out.push(a[i]+','+(can?'既存証跡で確定可能':'追加取得必須')+','+reason);}fs.writeFileSync(p+'P0候補26件_確定可能性一覧.csv',out.join('\n'));console.log(out.length-1);
