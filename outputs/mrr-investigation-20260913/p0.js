const fs=require('fs');const p='outputs/mrr-investigation-20260913/';let lines=fs.readFileSync(p+'重複候補_根拠補完.csv','utf8').trim().split(/\r?\n/);let h=lines.shift().split(',');let out=[lines[0]&&'AccountId,AccountName,Contract,ContractName,Status,Start,End,MRR,Source,MonthlyRows,Invoices,Periods,EvidencePriority,ReviewReason,IntegrityStatus'];let n=0;for(const line of lines){let r=line.split(',');if(r[4]!=='Activated')continue;let mm=+r[9]||0,iv=+r[10]||0,pe=+r[11]||0;let bad=[];if(mm===0)bad.push('月次なし');if(iv===0)bad.push('請求なし');if(pe===0)bad.push('期間なし');if(bad.length){n++;out.push(line+',要確認,'+bad.join('|'));}}fs.writeFileSync(p+'P0候補_契約単位整合性確認表.csv',out.join('\n'));console.log('count',n);
